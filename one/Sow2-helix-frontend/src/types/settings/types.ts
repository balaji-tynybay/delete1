export type CardFilterItemsType = {
  displayText: string;
  isChecked: boolean;
}[];

export type BarFilterItemsType = {
  displayText: string;
  id: number;
}[];

export type GraphItemsType = {
  displayText: string;
  isChecked: boolean;
}[];

export type TicketItemsType = {
  displayText: string;
  isChecked: boolean;
}[];
